[phases.setup]
nixPkgs = ["nodejs_20", "python311", "tesseract"]

[phases.build]
cmds = [
    "npm install",
    "npm run build",
    "pip install -r requirements.txt"
]

[start]
cmd = "sh -c \"uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}\""
